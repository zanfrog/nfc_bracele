<!DOCTYPE html>
<html lang="zh-Hant">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<title>簪蛙&米蛙幸運手串</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
  :root{
    --ink:#1E2126;
    --charcoal:#24282F;
    --charcoal-2:#343941;
    --paper:#F6F5F1;
    --mist:#8B9097;
    --slate:#5B7280;
    --gold:#C9A24B;
    --hairline:rgba(30,33,38,0.14);
    --hairline-light:rgba(255,255,255,0.14);
  }
  *{box-sizing:border-box;}
  html,body{margin:0;padding:0;height:100%;}
  body{
    font-family:'Noto Sans TC',sans-serif;
    min-height:100vh;
    display:flex;justify-content:center;align-items:center;
    background:var(--charcoal);
    position:relative;overflow:hidden;
    padding:20px 16px;
  }
  body::before{
    content:"";position:fixed;inset:0;z-index:0;pointer-events:none;
    background:
      repeating-linear-gradient(115deg, rgba(255,255,255,0.025) 0 1px, transparent 1px 10px),
      radial-gradient(ellipse at 50% -10%, rgba(255,255,255,0.05), transparent 55%);
  }
  .gold-line{position:fixed;top:0;left:0;right:0;height:3px;z-index:3;
    background:linear-gradient(90deg, transparent, var(--gold) 20%, #EAD48F 50%, var(--gold) 80%, transparent);}

  .stage{position:relative;z-index:2;width:100%;max-width:340px;}

  .brandbar{
    display:flex;align-items:center;justify-content:center;gap:8px;
    padding:10px 0 16px;
  }
  .brandbar .rule{height:1px;width:26px;background:var(--hairline-light);}
  .brandbar span{
    font-size:0.66rem;letter-spacing:0.22em;color:#C7CBD1;font-weight:500;
    white-space:nowrap;
  }

  .panel{
    background:var(--paper);
    border:1px solid var(--hairline);
    border-radius:6px;
    overflow:hidden;
  }
  .panel-head{
    display:flex;align-items:center;justify-content:space-between;
    padding:14px 18px;
    background:var(--charcoal-2);
    border-bottom:1px solid var(--hairline-light);
  }
  .badge{
    width:34px;height:34px;border-radius:50%;
    background:var(--charcoal);
    border:1px solid rgba(255,255,255,0.22);
    display:flex;align-items:center;justify-content:center;
    flex-shrink:0;
  }
  .badge svg{width:19px;height:19px;}
  #date-display{
    font-size:0.72rem;letter-spacing:0.1em;color:#D8DBDF;font-weight:400;margin:0;
  }
  .spec-tag{
    font-size:0.6rem;letter-spacing:0.14em;color:var(--gold);
    border:1px solid rgba(201,162,75,0.5);
    padding:3px 8px;border-radius:3px;
  }

  .panel-body{
    padding:32px 26px 28px;
    display:flex;flex-direction:column;align-items:center;text-align:center;
    min-height:230px;justify-content:center;
  }

  .draw-btn{
    background:transparent;
    color:var(--ink);
    border:1px solid var(--ink);
    padding:13px 30px;
    font-family:'Noto Sans TC',sans-serif;
    font-size:0.92rem;font-weight:500;letter-spacing:0.06em;
    border-radius:3px;
    cursor:pointer;
    position:relative;overflow:hidden;
    transition:background 0.2s ease, color 0.2s ease;
  }
  .draw-btn:active{background:var(--ink);color:var(--paper);}
  .draw-btn .ripple{
    position:absolute;border-radius:50%;
    background:rgba(30,33,38,0.18);
    transform:scale(0);
    animation:rippleGo 0.6s ease-out forwards;
    pointer-events:none;
  }
  @keyframes rippleGo{to{transform:scale(3.2);opacity:0;}}

  #ritual-stage{display:none;width:100%;}
  #ritual-stage.active{display:flex;flex-direction:column;align-items:center;gap:10px;}
  .weave-track{
    width:100%;max-width:180px;height:3px;border-radius:2px;
    background:var(--hairline);overflow:hidden;position:relative;
  }
  .weave-track::after{
    content:"";position:absolute;top:0;left:-40%;width:40%;height:100%;
    background:repeating-linear-gradient(115deg, var(--slate) 0 4px, var(--gold) 4px 8px);
    animation:weaveMove 1s ease-in-out infinite;
  }
  @keyframes weaveMove{
    0%{left:-40%;}
    100%{left:100%;}
  }
  .weave-label{font-size:0.68rem;letter-spacing:0.12em;color:var(--mist);}

  #result-container{display:none;width:100%;}
  #result-container.show{display:flex;flex-direction:column;align-items:center;}

  .reveal-item{opacity:0;transform:translateY(8px);}
  .reveal-item.play{animation:revealUp 0.5s ease forwards;}
  @keyframes revealUp{to{opacity:1;transform:translateY(0);}}

  .legend-row{
    display:flex;align-items:center;gap:8px;
    font-size:0.82rem;color:var(--ink);font-weight:500;
    margin-bottom:22px;
  }
  #color-box{
    width:22px;height:22px;border-radius:50%;
    border:1px solid rgba(30,33,38,0.3);
    flex-shrink:0;
  }
  #color-box.pop{animation:dotPop 0.4s ease forwards;}
  @keyframes dotPop{0%{transform:scale(0);}100%{transform:scale(1);}}
  .highlight-color{font-weight:600;}

  .spec-block{
    width:100%;
    border-top:1px solid var(--hairline);
    border-bottom:1px solid var(--hairline);
    padding:16px 0;
    margin-bottom:22px;
  }
  .spec-block .spec-label{
    font-size:0.62rem;letter-spacing:0.18em;color:var(--mist);margin-bottom:6px;
  }
  #number-val{
    font-size:2.4rem;font-weight:300;color:var(--ink);letter-spacing:0.02em;line-height:1;
  }
  #number-seal.stamp #number-val{animation:numIn 0.4s ease forwards;}
  @keyframes numIn{0%{opacity:0;transform:translateY(4px);}100%{opacity:1;transform:translateY(0);}}

  #wish{
    font-family:"標楷體","DFKai-SB","BiauKai","STKaiti","KaiTi",serif;
    font-size:1.14rem;line-height:1.75;
    color:var(--ink);
    padding:14px 16px;
    border:1px solid var(--hairline);
    border-radius:4px;
    background:rgba(91,114,128,0.05);
  }

  .redraw-note{
    margin-top:16px;font-size:0.68rem;letter-spacing:0.06em;color:var(--mist);
  }
</style>
</head>
<body>

<div class="gold-line"></div>

<div class="stage">
  <div class="brandbar">
    <div class="rule"></div>
    <span>ZHAN FROG &amp; MI FROG</span>
    <div class="rule"></div>
  </div>

  <div class="panel">
    <div class="panel-head">
      <div style="display:flex;align-items:center;gap:10px;">
        <div class="badge">
          <svg viewBox="0 0 40 40" fill="none">
            <circle cx="20" cy="20" r="18" stroke="#F6F5F1" stroke-width="1.1" opacity="0.55"/>
            <ellipse cx="20" cy="24" rx="10" ry="7.5" stroke="#F6F5F1" stroke-width="1.3" fill="none"/>
            <circle cx="13" cy="14" r="4.4" stroke="#F6F5F1" stroke-width="1.3" fill="none"/>
            <circle cx="27" cy="14" r="4.4" stroke="#F6F5F1" stroke-width="1.3" fill="none"/>
            <circle cx="13" cy="14" r="1.3" fill="#F6F5F1"/>
            <circle cx="27" cy="14" r="1.3" fill="#F6F5F1"/>
          </svg>
        </div>
        <p id="date-display">今日能量</p>
      </div>
      <span class="spec-tag">DAILY</span>
    </div>

    <div class="panel-body">
      <button id="draw-btn" class="draw-btn">輕點蛙蛙，抽今日運勢</button>

      <div id="ritual-stage">
        <div class="weave-track"></div>
        <div class="weave-label">織入今日能量中</div>
      </div>

      <div id="result-container">
        <div class="legend-row reveal-item" id="color-name-row">
          <div id="color-box"></div>
          <span id="color-name">幸運色：<span class="highlight-color">-</span></span>
        </div>

        <div class="spec-block reveal-item" id="number-seal">
          <div class="spec-label">幸運號碼</div>
          <div id="number-val">-</div>
        </div>

        <p id="wish" class="reveal-item"></p>
        <p id="redraw-note" class="redraw-note" style="display:none;">今日的緣分已經抽過囉，明天再來一次吧</p>
      </div>
    </div>
  </div>
</div>

<script>
  const now = new Date();
  const year = now.getFullYear();
  const month = now.getMonth() + 1;
  const date = now.getDate();
  const days = ["星期日", "星期一", "星期二", "星期三", "星期四", "星期五", "星期六"];
  const todayStr = `${month}月${date}日 ${days[now.getDay()]}`;
  const dateKey = `${year}-${month}-${date}`;

  document.getElementById('date-display').innerText = todayStr;

  const btn = document.getElementById('draw-btn');
  const ritualStage = document.getElementById('ritual-stage');
  const resultContainer = document.getElementById('result-container');
  const redrawNote = document.getElementById('redraw-note');

  let alreadyDrawnToday = false;
  try {
    const saved = localStorage.getItem('myLuckyBracelet');
    if (saved) {
      const parsed = JSON.parse(saved);
      if (parsed.savedDate === dateKey) {
        alreadyDrawnToday = true;
        btn.innerText = '輕點蛙蛙，看看今日運勢';
      }
    }
  } catch (e) {}

  function spawnRipple(e) {
    const r = document.createElement('span');
    r.className = 'ripple';
    const rect = btn.getBoundingClientRect();
    const size = Math.max(rect.width, rect.height);
    r.style.width = r.style.height = size + 'px';
    r.style.left = (e.clientX - rect.left - size / 2) + 'px';
    r.style.top = (e.clientY - rect.top - size / 2) + 'px';
    btn.appendChild(r);
    setTimeout(() => r.remove(), 650);
  }

  function playReveal(luckyData) {
    document.getElementById('color-box').style.backgroundColor = luckyData.color.hex;
    document.getElementById('color-name').innerHTML = `幸運色：<span class="highlight-color">${luckyData.color.name}</span>`;
    document.getElementById('number-val').innerText = luckyData.num;
    document.getElementById('wish').innerText = "「" + luckyData.sentence.text + "」";

    resultContainer.classList.add('show');
    if (alreadyDrawnToday) redrawNote.style.display = 'block';

    const colorRow = document.getElementById('color-name-row');
    const colorBox = document.getElementById('color-box');
    const seal = document.getElementById('number-seal');
    const wish = document.getElementById('wish');

    colorRow.classList.add('play');
    colorBox.classList.add('pop');
    setTimeout(() => { seal.classList.add('play', 'stamp'); if (navigator.vibrate) navigator.vibrate(60); }, 260);
    setTimeout(() => { wish.classList.add('play'); }, 520);
  }

  btn.addEventListener('click', async function (e) {
    if (navigator.vibrate) navigator.vibrate(200);
    spawnRipple(e);

    btn.style.display = 'none';

    const skipHop = alreadyDrawnToday;

    if (!skipHop) {
      ritualStage.classList.add('active');
    }

    try {
      let luckyData = null;
      const savedData = localStorage.getItem('myLuckyBracelet');

      if (savedData) {
        const parsedData = JSON.parse(savedData);
        if (parsedData.savedDate === dateKey) {
          luckyData = parsedData;
        }
      }

      const fetchPromise = (async () => {
        if (luckyData) return luckyData;
        const colors = await fetch('color.json').then(res => res.json());
        const sentences = await fetch('sentence.json').then(res => res.json());
        const newColor = colors[Math.floor(Math.random() * colors.length)];
        const newSentence = sentences[Math.floor(Math.random() * sentences.length)];
        const newNum = Math.floor(Math.random() * 99) + 1;
        const data = { savedDate: dateKey, color: newColor, sentence: newSentence, num: newNum };
        localStorage.setItem('myLuckyBracelet', JSON.stringify(data));
        return data;
      })();

      const minWait = skipHop ? 200 : 900;
      const [finalData] = await Promise.all([
        fetchPromise,
        new Promise(res => setTimeout(res, minWait))
      ]);

      ritualStage.classList.remove('active');
      playReveal(finalData);

    } catch (error) {
      console.error("讀取失敗:", error);
      ritualStage.classList.remove('active');
      resultContainer.classList.add('show');
      document.getElementById('wish').innerText = "讀取資料時發生錯誤，請檢查檔案路徑。";
      document.getElementById('wish').classList.add('play');
    }
  });
</script>
</body>
</html>